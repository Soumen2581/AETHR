AETHR macOS distribution

This folder holds packaging metadata. Installers are produced into dist/macos/
by Tools/package_macos.sh (CMake target: package-macos).

The DMG contains:
  • Install AETHR.pkg  — system installer (Applications + VST3 + AU)
  • Applications       — shortcut
  • README.txt
